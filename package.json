{
  "name": "ai-employee-platform",
  "version": "1.0.0",
  "private": true,
  "description": "AI Employee Platform — Digital Workforce Operating System (500 Canonical Role Packs)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); ['packages', 'apps'].forEach(dir => { if (fs.existsSync(dir)) fs.readdirSync(dir).forEach(p => { ['dist', '.next', 'tsconfig.tsbuildinfo'].forEach(sub => { const target = path.join(dir, p, sub); if (fs.existsSync(target)) fs.rmSync(target, { recursive: true, force: true }); }); }); }); if (fs.existsSync('tsconfig.tsbuildinfo')) fs.rmSync('tsconfig.tsbuildinfo', { force: true });\"",
    "typecheck": "tsc -b",
    "build:packages": "tsc -b",
    "build:web": "npm run build --workspace=apps/web",
    "build": "npm run build:packages && npm run build:web",
    "test:catalog": "npm run test --workspace=packages/rolepack",
    "test:runtime": "npm run test --workspace=packages/runtime",
    "test:security": "npm run test --workspace=packages/policies",
    "test:billing": "npm run test --workspace=packages/marketplace-billing",
    "test:tools": "npm run test --workspace=packages/tool-sdk",
    "eval:catalog": "npm run eval:catalog --workspace=packages/evaluation-sdk",
    "test": "npm run test:catalog && npm run test:runtime && npm run test:security && npm run test:billing && npm run test:tools && npm run eval:catalog",
    "verify": "npm run clean && npm run build:packages && npm test",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "caniuse-lite": "^1.0.30001810",
    "typescript": "^5.9.3"
  }
}
